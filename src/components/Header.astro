---
import Navigation from "@components/Navigation.astro";
import Hamburger from "@components/Hamburger.astro";
import ThemeIconAndToggle from "@components/ThemeIconAndToggle.astro";
const { activeLink } = Astro.props;
---

<nav>
  <span class="hamburger">
    <Hamburger />
  </span>
  <Navigation activeLink={activeLink} />
  <span class="icon-container">
    <ThemeIconAndToggle />
  </span>
</nav>
<script>
  document.querySelector(".hamburger")?.addEventListener("click", () => {
    document.querySelector(".nav-links")?.classList.toggle("expanded");
    document.querySelector(".icon-container")?.classList.toggle("expanded");
  });
</script>

<style>
  nav {
    width: 100%;
    background-color: var(--light-bg-color);
    position: fixed;
    display: flex;
    justify-content: space-between;
    align-items: center;
    top: 0;
    height: 71px;
    z-index: 101;
  }

  .icon-container {
    padding-right: 30px;
  }

  .hamburger {
    align-self: center;
  }

  .nav-links {
    align-self: flex-end;
    border: 1px solid red;
  }

  .expanded {
    display: none;
  }

  html.dark nav {
    background-color: var(--color-night-bg);
  }

  @media screen and (min-width: 687px) {
    nav {
      top: unset;
      height: 90px;
      max-width: 64ch;
      justify-content: unset;
    }

    .nav-links {
      flex-grow: 1;
    }

    .icon-container {
      margin: 14px 10px 10px 10px;
      flex-grow: 0;
    }
  }
</style>
